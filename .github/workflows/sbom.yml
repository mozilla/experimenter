name: ssdlc-sbom

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions: {}

jobs:
  sbom:
    permissions:
      actions: read
      contents: write
    uses: mozilla/ssdlc-actions/.github/workflows/ssdlc-sbom.yml@0980dd214a92599ecdb67ad30f7086b508a3f825
    with:
      sbom_name: ${{ github.sha }}
