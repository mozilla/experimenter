name: ssdlc-sbom

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions: {}

jobs:
  sbom:
    permissions:
      actions: read
      contents: write
    uses: mozilla/ssdlc-actions/.github/workflows/ssdlc-sbom.yml@d355de9a0cb79e0be2599696064231ee49aebb92
    with:
      sbom_name: ${{ github.sha }}
